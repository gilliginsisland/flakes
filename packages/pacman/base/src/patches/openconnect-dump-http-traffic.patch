diff --git a/library.c b/library.c
index 840846d..786cff5 100644
--- a/library.c
+++ b/library.c
@@ -495,4 +495,5 @@ void openconnect_set_loglevel(struct openconnect_info *vpninfo, int level)
 void openconnect_set_loglevel(struct openconnect_info *vpninfo, int level)
 {
+	vpninfo->dump_http_traffic = level >= PRG_DEBUG;
	vpninfo->verbose = level;
 }
diff --git a/http.c b/http.c
index ca16582..22e9efd 100644
--- a/http.c
+++ b/http.c
@@ -259,2 +259,5 @@ int process_http_response(struct openconnect_info *vpninfo, int connect,
 
+	if (vpninfo->dump_http_traffic && hdrbuf->pos)
+		dump_buf(vpninfo, '<', hdrbuf->data);
+
 	if (!strncmp(hdrbuf->data, "HTTP/1.0 ", 9))
@@ -285,2 +288,5 @@ int process_http_response(struct openconnect_info *vpninfo, int connect,
 
+		if (vpninfo->dump_http_traffic)
+			dump_buf(vpninfo, '<', hdrbuf->data);
+
 		/* Empty line ends headers */
diff --git a/cstp.c b/cstp.c
index b459356..ff4ea27 100644
--- a/cstp.c
+++ b/cstp.c
@@ -360,2 +360,5 @@ static int start_cstp_connection(struct openconnect_info *vpninfo, int strap_rek
 
+	if (vpninfo->dump_http_traffic)
+		dump_buf(vpninfo, '<', buf);
+
 	if (strncmp(buf, "HTTP/1.1 200 ", 13)) {
@@ -403,2 +406,5 @@ static int start_cstp_connection(struct openconnect_info *vpninfo, int strap_rek
 
+		if (vpninfo->dump_http_traffic)
+			dump_buf(vpninfo, '<', buf);
+
 		colon = strchr(buf, ':');
